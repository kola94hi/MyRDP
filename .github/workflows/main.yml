name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force

          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          # Generar contraseña más simple y visible
          $password = "RDP123!password"
          
          # Crear usuario
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDPUser" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDPUser"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDPUser"
          
          # Mostrar contraseña CLARAMENTE en los logs
          Write-Host "=========================================="
          Write-Host "CREDENCIALES RDP - COPIA Y PEGA:"
          Write-Host "USUARIO: RDPUser"
          Write-Host "CONTRASEÑA: RDP123!password"
          Write-Host "=========================================="
          
          # También guardar en variables
          echo "RDP_USER=RDPUser" >> $env:GITHUB_ENV
          echo "RDP_PASSWORD=RDP123!password" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
          Write-Host "Tailscale IP: $tsIP"

      - name: Display Final Connection Info
        run: |
          Write-Host ""
          Write-Host "🎯 CONEXIÓN RDP LISTA - USA ESTAS CREDENCIALES:"
          Write-Host "================================================"
          Write-Host "📍 DIRECCIÓN: $env:TAILSCALE_IP"
          Write-Host "👤 USUARIO: RDPUser"
          Write-Host "🔑 CONTRASEÑA: RDP123!password"
          Write-Host "================================================"
          Write-Host ""
          Write-Host "📝 INSTRUCCIONES:"
          Write-Host "1. Abre 'Conexión a Escritorio Remoto'"
          Write-Host "2. Pega la dirección: $env:TAILSCALE_IP"
          Write-Host "3. Usuario: RDPUser"
          Write-Host "4. Contraseña: RDP123!password"
          Write-Host ""

      - name: Wait 10 Minutes
        run: |
          $endTime = (Get-Date).AddMinutes(10)
          Write-Host "⏰ Sesión activa por 10 minutos - Termina a las $($endTime.ToString('HH:mm:ss'))"
          
          while ((Get-Date) -lt $endTime) {
              $remaining = $endTime - (Get-Date)
              $minutes = [math]::Floor($remaining.TotalMinutes)
              $seconds = $remaining.Seconds
              Write-Host "Tiempo restante: $minutes min $seconds seg"
              Start-Sleep -Seconds 30
          }
          Write-Host "🚫 Sesión RDP finalizada"
